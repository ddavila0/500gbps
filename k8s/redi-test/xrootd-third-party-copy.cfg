cms.space min 2g 5g

acc.authdb /etc/xrootd/auth-file-noauth
all.role manager
set xrdr = $POD_IP
all.manager $xrdr:9051 

all.export /

oss.localroot /mnt/ramdisk
if defined ?~SITENAME
  set sitename = $SITENAME
else
  set sitename = xrootd-redi
fi
all.sitename $sitename
